services:
  nginx:
    image: nginx:1.27-alpine
    container_name: ${LB_CONTAINER_NAME:-encaja-login-real-lb}
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
